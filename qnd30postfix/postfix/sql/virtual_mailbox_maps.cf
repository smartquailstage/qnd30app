user = sqadmindb
password = smartquaildev1719pass
dbname = POSFIXDB
hosts = smartquaildb

query = SELECT CONCAT('/var/mail/', vd.domain, '/', vu.email, '/Maildir/')
FROM virtual_users vu
JOIN virtual_domains vd ON vu.domain_id = vd.id
WHERE vu.email='%s';
