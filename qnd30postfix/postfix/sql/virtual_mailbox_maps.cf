user = sqadmindb
password = smartquaildev1719pass
dbname = POSFIXDB
hosts = smartquaildb

# Query para obtener la ruta del buzón de correo
query = SELECT CONCAT('/var/mail/', SUBSTRING_INDEX(email, '@', -1), '/', SUBSTRING_INDEX(email, '@', 1), '/') FROM virtual_users WHERE email='%s'

